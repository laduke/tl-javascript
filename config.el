(defvar javascript-backend 'tide
  "The backend to use for IDE features. Possible values are `tide'.")

(defvar javascript-fmt-tool 'prettier
  "The formatter to format a JavaScript file. Possible values are `prettier'.")

(defvar javascript-fmt-on-save nil
  "Run formatter on buffer save.")
